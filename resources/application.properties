db.url=jdbc:postgresql://localhost:5432/postgres
db.username=postgres
db.password=myPass
db.Drv=org.postgresql.Driver
db.dial=org.hibernate.dialect.PostgreSQL10Dialect
db.crTab=CREATE TABLE IF NOT EXISTS userTable (id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,name VARCHAR(255),lastname VARCHAR(255),age SMALLINT)
db.drTab=DROP TABLE IF EXISTS USERTABLE
db.clTab=DELETE FROM User
db.selUser=SELECT FROM userTable